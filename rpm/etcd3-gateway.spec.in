%{!?python_sitelib: %define python_sitelib %(%{__python} -c "from distutils.sysconfig import get_python_lib; print get_python_lib()")}

Name:           etcd3-gateway
Summary:        etcd3 gateway Python Client
Version:
Release:
License:        Apache-2
URL:            https://github.com/dims/etcd3-gateway
Source0:        etcd3-gateway-%{version}.tar.gz
BuildArch:	noarch
Group:          Applications/Engineering
Requires:       python-pbr


%description
A python client for etcd3 grpc-gateway v3alpha API


%description
This package installs the etcd3-gateway Python code.

%files
%defattr(-,root,root,-)
%{python_sitelib}/etcd3gw*
%doc


%prep
%setup -q


%build
%{__python} setup.py build


%install
rm -rf $RPM_BUILD_ROOT
%{__python} setup.py install -O1 --skip-build --root $RPM_BUILD_ROOT


%clean
rm -rf $RPM_BUILD_ROOT


%changelog
